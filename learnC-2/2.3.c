/* Computes the dimensional weight of a 12'' x 10'' x 8'' box */

#include <stdio.h>

int main(void)
{
    int a, b, c, h, i, j;
    float e, f, g;
    printf("%d, %d, %d, %d, %d, %d \n", a, b, c, h, i, j);
    printf("%f, %f, %f", e, f, g);
    return 0;
}